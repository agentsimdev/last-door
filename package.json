{
  "name": "agentsim-last-door",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173",
    "build:assets": "node scripts/build-workers-assets.mjs",
    "dev:workers": "npm run build:assets && wrangler dev --env '' --local --config wrangler.jsonc",
    "check:workers": "npm run build:assets && node --test workers.test.mjs",
    "build:workers": "npm run build:assets && wrangler deploy --config wrangler.jsonc --env production --autoconfig=false --dry-run",
    "deploy:production": "npm run build:assets && wrangler deploy --config wrangler.jsonc --env production --autoconfig=false",
    "test": "node --test domain.test.mjs native.test.mjs",
    "check": "node --check app.mjs && node --test domain.test.mjs native.test.mjs"
  },
  "devDependencies": {
    "wrangler": "4.131.2"
  }
}
