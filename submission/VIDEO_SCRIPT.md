# Demo video script

Current public render: 1 minute 32.133 seconds. Local iteration candidate: 1 minute 38.871 seconds; not yet rendered or published.

## 0:00 to 0:15

Visual: Open tight on `09`, then reveal the exact production counterfactual: the same evidence produces a static nine-tool surface or an evidence-compiled four-tool surface. Land on `5 stale capabilities removed`.

Voiceover: Can a browser agent know when not to act? Both sides have the same evidence. The left keeps all nine tools exposed, even after five go stale. LAST DOOR rebuilds the toolset, leaving only four.

## 0:15 to 0:20

Visual: Show the human-only boundary, the official AgentSIM lockup, and the three proof stations: `NEVER REGISTERED`, `PERSON PRESENT`, `AGENT STOPS`.

Voiceover: And the final action never becomes a tool. That stays with the person.

## 0:20 to 0:33

Visual: Reset the mission. Show the three initial native tools and give the prompt to the agent.

Voiceover: The page starts with a versioned authority decision. That decision names the actor, the rule, the trusted evidence, and the capabilities WebMCP may expose.

## 0:33 to 0:46

Visual: Clear door 01. At door 02, show the expired event, rejected resolution, and `STALE_CHALLENGE_REJECTED` entering Run Memory without a challenge value.

Voiceover: Door one clears. Then the first challenge arrives expired. The page rejects it and records stale challenge rejected, without returning the challenge value or putting it in Run Memory.

## 0:46 to 0:55

Visual: Show the fresh event, page-held resolution, and door 02 clearing while the evidence chain remains intact.

Voiceover: The agent waits again. A fresh event arrives, the page-held challenge resolves, and door two clears. The evidence chain stays intact.

## 0:55 to 1:10

Visual: Show door 03, the handoff request, and the result of `explain_authority_decision`.

Voiceover: Door three belongs to a person. The rule is human handoff pending, and the actor is human. Seven redacted facts show exactly how the run reached this point. No confirmation capability exists, so the agent stops.

## 1:10 to 1:25

Visual: The presenter clicks the human button. Show the final receipt, `RUN_COMPLETE`, and eight evidence facts.

Voiceover: I perform the one human action. The receipt records three gates passed, one safe recovery, one human handoff, eight evidence facts, and zero unauthorized attempts. The final rule is run complete.

## 1:25 to 1:39

Visual: Cut to the native test bench showing a pass, then the LAST DOOR title. Use the 30/30 result to ask the harder question: does the same compiler transfer? Show identity recovery `09 → 04`, high-value checkout `10 → 04`, and production change `08 → 04`. End on the receipt: `3 policy packs / 15 stale capabilities removed / 0 human actions registered`. Keep the isolated-policy-snapshot scope visible.

Voiceover: Thirty public rehearsals passed. LAST DOOR proves that stopping can be the correct completion.

Audio status: the new transfer evidence is a music-only coda in the local candidate. A same-voice narration replacement is blocked by exhausted HeyGen free TTS allowance; the current public video remains unchanged.
